import { BorderedFlexContainer } from '../../../common';

import styled from 'styled-components';

const StyledFormGroupWrapper = styled(BorderedFlexContainer)`
	min-width: 300px;
`;

export default StyledFormGroupWrapper;
